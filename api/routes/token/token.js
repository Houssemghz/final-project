// const jwt = require("jsonwebtoken");
// require("dotenv").config();

// export const AccessToken = (user) => {
//   return jwt.sign({ id: user._id, email: user.email }, process.env.tokenKey, {
//     expiresIn: "365d",
//   });
// };
